---
title: Mean Value Theorem
description: Visualization of the Mean Value Theorem.
visible: true
approved: true
category: Differentiation
---
import MathBlock from "../../components/MathBlock";
import MeanValueTheorem from "../../components/applets/MeanValueTheorem";

# Mean Value Theorem

<MathBlock type="theorem" title="Mean Value Theorem">
    Let $f : [a,b] \to \mathbb{R}$ be continuous on $[a,b]$ and differentiable on $(a,b)$. Then there exists a $c \in (a,b)$ such that 
    $$
    f'(c) = \frac{f(b) - f(a)}{b - a}.
    $$
</MathBlock>

--- 
<MeanValueTheorem client:load />
--- 

## Description
[Link to code](https://github.com/daniele-bolla/math-applets/blob/main/src/components/applets/MeanValueTheorem.tsx).

To demonstrate the theorem i have selected a simple function $0.2*x^3 - 0.5*x^2 + 2$ with derivative $0.6x^2 - x$.
```typescript
    const f = (x: number) => 0.2 * x * x * x - 0.5 * x * x + 2;
    // const fPrime = (x: number) => 0.6 * x * x - x;
```
Points $a$ and $b$ can be adjusted. The secant is just a line passing though $f(a)$ and $f(b)$.
To construct $c$ i solved the equation:
$$
f'(c) = \frac{f(b) - f(a)}{b-a} = \text{secantSlope} 
$$
Giving $c^2 - c - \frac{f(b) - f(a)}{(b-a)*0.6}$.
Solving with the quadratric formula: $c =1 ± \frac{\sqrt{1 + 4*0.6*\text{secantSlope}}}{2*0.6}$.
If the discriminant is negative or $c$ does not lie on the interval $[a,b]$ i return `NaN` (not a number).  
```typescript
    function findC(): number {
        const a = pointA.X();
        const b = pointB.X();
        const fa = f(a);
        const fb = f(b);
        const secantSlope = (fb - fa) / (b - a);
        const discriminant = 1 + 4 * 0.6 * secantSlope;
        if (discriminant < 0) {
            return NaN;
        }
        const cPos = (1 + Math.sqrt(discriminant)) / (2 * 0.6);
        const cNeg = (1 - Math.sqrt(discriminant)) / (2 * 0.6);
        const minAB = Math.min(a, b);
        const maxAB = Math.max(a, b);
        if (cPos >= minAB && cPos <= maxAB) {
            return cPos;
        } else if (cNeg >= minAB && cNeg <= maxAB) {
            return cNeg;
        } else {
            return NaN;
        }
    }
```

Finally the tangent line (best approximation of $f'(c)$) has to be parallel to the secant.
```typescript
    board.create('parallel', [secant, pointC],...)
```